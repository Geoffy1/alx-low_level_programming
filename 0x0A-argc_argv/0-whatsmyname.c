#include "main.h"

/**
* main - prints name
*@argc: number of arguments
*@argv: array of arguments
*Return: 0forsuccess always
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
