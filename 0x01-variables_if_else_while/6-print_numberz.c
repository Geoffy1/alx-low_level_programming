#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - prints all single digit numbers
* Return: 0
*/
int main(void)
{
int ch;
for (ch = 48; ch < 57; ch++)
{
putchar(ch);
}
putchar(10);
return (0);
}
